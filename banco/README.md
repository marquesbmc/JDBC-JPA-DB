# Banco da livraria

`livraria-base.db` e um banco SQLite pronto para a aula. Possui `autor`, `livro`, `venda` e `item_venda`, quatro autores e oito livros com precos e estoques variados.

Nenhuma classe de aula cria a estrutura do banco.